<% include ../partials/header %>

<div id="user-lists">
  <% lists.forEach(list => { %>
    <div class="listTitle">
      <h1><%- list.title %> List:</h1>
    </div>
    <div class="tasks">
      <h2>Tasks:</h2>
        <% tasks_in_list.forEach(list => { %>
          <div class="task-list">
          </div>
        <% }) %>
    </div>
  <% }) %>
</div>

<% include ../partials/footer %>
